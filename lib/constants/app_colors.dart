import 'package:flutter/material.dart';

/// all app colors
class AppColors {
  /// black color used in the drawer items
  static const Color darkBlack = Colors.black;

  /// text white color - White
  static const Color textWhite = Color(0xFFFFFFFF);

  /// background white color - White
  static const Color backgroundWhite = Color(0xFFFFFFFF);

  /// completely invisible.
  static const Color completeTransparent = Color(0x00000000);

  /// app snackbar color - color
  static const Color snackbarColor = Color(0xFFC24530);

  /// text black color like footer text in splash page
  static const Color textBlack = Color(0xFF000000);

  /// error color red
  static const Color errorColor = Color(0xFFF44444);

  /// light gray color
  static const Color lightGrayColor = Colors.grey;

  /// app splash color
  static const Color appSplashColor = Color(0xFF052647);
}
